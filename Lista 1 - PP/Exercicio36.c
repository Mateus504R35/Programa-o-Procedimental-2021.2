#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main () // exercicio 36
{
    float h, r, v, pi;
    printf("Escreva o valor da altura e do raio de um cilindro circular: ");
    scanf("%f %f", &h, &r);
    pi = 3.141592;
    v = pi*(r*r)*h;
    printf("O volume do cilindro circular é: %f", f);

    return 0;

}