#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/*
int main()   // exercicio 1
{
    int a, b;
    printf("Escreva dois numeros inteiros: ");
    scanf("%d %d", &a, &b);

    if (a > b)
    {
        printf("O primeiro número é o maior");
    }
    else
    {
      printf("O segundo número é o maior");
    }

    return 0;

}


int main () // exercicio 2
{
    int a;
    printf("Escreva um numero: ");
    scanf("%d", &a);

    if (a >= 0)
        {
            double x;
            x = sqrt(a);
            printf("A raiz quadrada desse numero é: %.2f", x);
        }
    else
        {
            printf("Erro, escreva um numero positivo");
        }

    return 0;
}


int main () // exercicio 3
{
    float a;
    printf("Escreva um numero: ");
    scanf("%f", &a);

    if (a >= 0)
        {
            double x;
            x = sqrt(a);
            printf("A raiz quadrada desse numero é: %.2f", x);
        }
    else
        {
            printf("O dobro desse numero é: %.2f", a*a);
        }

    return 0;
}

int main () // exercicio 4
{
    int a;
    printf("Escreva um numero: ");
    scanf("%d", &a);

    if (a >= 0)
        {
            double x, y;
            x = sqrt(a);
            y = a*a;

            printf("A raiz quadrada desse numero é: %.2f e seu quadrado é: %.2f", x, y);

        }
    else
        {
            printf("Erro");
        }

   return 0;
}

int main () // exercicio 5
{
    int a;
    printf("Escreva um numero: ");
    scanf("%d", &a);

    if (a % 2 == 0)
        {
            printf("Esse numero é par.");
        }
    else
        {
            printf("Esse numero é impar.");
        }

    return 0;
}

int main () // exercicio 6
{
        int a, b;
        printf("Escreva dois numeros: ");
        scanf("%d %d", &a, &b);

        if (a > b)
            {
                printf("O numero %d é o maior e a diferença entre os dois numeros digitados é %d .", a, a-b);
            }
        else
            {
                printf("O numero %d é o maior e a diferença entre os dois numeros digitados é %d .", b, b-a);
            }

    return 0;
}

int main ()  // exercicio 7
{
        int a, b;
        printf("Escreva dois numeros: ");
        scanf("%d %d", &a, &b);
        if (b > a)
            {
                printf("O maior numero é %d", b);
            }
        else
            {
                if (a > b)
                {
                    printf("O maior numero eh %d", a);
                }
                else
                {
                    printf("Os numeros são iguais ");
                }
            }

            return 0;
}

int main () // exercicio 8
{
    float a, b;
    printf("Escreva duas notas: ");
    scanf("%f %f", &a, &b);

    if ((a >= 0.0 && a <= 10.0) && (b >= 0.0 && b <= 10.0))
    {
        printf("A media dessas notas eh: %.2f", (a+b)/2);
    }
    else
    {
        printf("Não existe nota valida.");
    }

    return 0;
}

int main () // exercicio 9
{
    int s, p;
    printf("Escreva seu salario:");
    scanf("%d", &s);
    printf("Escreva o valor da prestacao do emprestimo:");
    scanf("%d", &p);

    if (p > (0.20*s))
    {
        printf("Emprestimo não concedido");
    }
    else
    {
        printf("Emprestimo concedido");
    }

    return 0;
}

int main () // exercicio 10
{
    float h;
    char s;
    printf("Escreva a altura em metros: ");
    scanf("%f", &h);
    printf("Agora escreva H se for homem ou M se for mulher: ");
    scanf("%ch", &s);

    if ((s == 'H') || (s == 'h'))
    {
        float peso;
        peso = (72.7*h) - 58.0;
        printf("Seu peso ideal é: %.2f", peso);
    }
    else
    {
        float peso;
        peso = (62.1*h) - 44.7;
        printf("Seu peso ideal é: %.2f", peso);
    }

    return 0;
}

int main () // exercicio 11
{
    int a, b, c, d;
    printf("Escreva um numero:");
    scanf("%d", &a);

    if (a >= 0)
    {
        b = a / 100;
        c = (a % 100)/10;
        d = (a % 100)%10;

       printf("A soma dos algarismos desse numero eh: %d", b+c+d);
    }
    else
    {
        printf("Numero invalido");
    }

    return 0;
}
*/








